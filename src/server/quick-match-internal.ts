// Headers used between the Worker entrypoint / MCP helpers and the
// MatchmakerDO internal `/enqueue` route. Not forwarded from browsers.

export const QUICK_MATCH_VERIFIED_AGENT_HEADER = 'X-Delta-V-Verified-Agent';
